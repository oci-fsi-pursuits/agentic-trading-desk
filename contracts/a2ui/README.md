# A2UI Contracts

This directory is reserved for optional declarative UI payload contracts.

Phase 0 does not define A2UI widgets yet. The first public demo can ship with conventional UI components backed by canonical AG-UI events and typed objects.
