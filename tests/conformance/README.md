# Conformance Fixtures

Phase 0 fixture coverage for the `single_name_earnings` scenario.

The parity harness should eventually:
1. execute the same scenario on WayFlow and LangGraph,
2. capture canonical AG-UI envelopes,
3. materialize typed objects,
4. compare stage sequence,
5. compare required final object IDs and stable fields.

This directory currently contains the minimum expected outputs and comparator profile for the starter scenario.
